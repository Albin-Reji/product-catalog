<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.6.xsd">

    <changeSet id="003-create-product-attributes" author="albin">

        <!--
            PRECONDITION: If the 'product_attributes' table already exists, mark this
            changeset as already run and skip it.
        -->
        <preConditions onFail="MARK_RAN">
            <not>
                <tableExists tableName="product_attributes"/>
            </not>
        </preConditions>

        <createTable tableName="product_attributes">

            <!-- UUID primary key — matches GenerationType.UUID in ProductAttribute.java -->
            <column name="attribute_id" type="varchar(36)">
                <constraints primaryKey="true"
                             primaryKeyName="pk_product_attributes"
                             nullable="false"/>
            </column>

            <!-- FK to products — matches @JoinColumn(name = "product_id") -->
            <column name="product_id" type="varchar(36)">
                <constraints nullable="false"/>
            </column>

            <!-- attr_key — matches @Column(name = "attr_key") -->
            <column name="attr_key" type="varchar(100)"/>

            <!-- attr_value — matches @Column(name = "attr_value") -->
            <column name="attr_value" type="text"/>

        </createTable>

        <!-- FK with ON DELETE CASCADE — matches @OnDelete(action = OnDeleteAction.CASCADE) -->
        <!-- When a product is deleted, all its attributes are deleted automatically -->
        <addForeignKeyConstraint
                baseTableName="product_attributes"
                baseColumnNames="product_id"
                referencedTableName="products"
                referencedColumnNames="product_id"
                constraintName="fk_attribute_product"
                onDelete="CASCADE"
                onUpdate="NO ACTION"/>

        <!-- Index on product_id — most queried column in this table -->
        <createIndex tableName="product_attributes" indexName="idx_attr_product_id">
            <column name="product_id"/>
        </createIndex>

        <!-- Unique constraint: one attr_key per product -->
        <!-- Prevents duplicate keys like two "color" rows for the same product -->
        <addUniqueConstraint
                tableName="product_attributes"
                columnNames="product_id, attr_key"
                constraintName="uq_product_attribute_key"/>

    </changeSet>

</databaseChangeLog>